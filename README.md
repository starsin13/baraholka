# Галерея

## Как добавить фото
1. Положите фото в папку `tovar1/`
2. Запустите `generate.ps1` (правой кнопкой → "Run with PowerShell")
3. Обновите страницу

## Как обновить сайт
```bash
git add .
git commit -m "Новые фото"
git push
```